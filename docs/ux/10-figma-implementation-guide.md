# Figma 设计稿实施指南

**版本**: v1.1  
**目标**: 将设计文档体系转化为可交付的Figma设计稿  
**更新日期**: 2025-11-01

---

## ⚡ 重要更新

**推荐使用 Figma Make (AI) 方案！**

```
手动方案：   60小时（1.5周全职）
AI辅助方案： 8-10小时（1-2天）  ⭐ 推荐

→ 节省时间：85%
→ 详细指南：见 11-figma-make-prompts.md
```

**本文档内容**：手动实施详细步骤（作为参考或AI生成后的调整指南）

---

## 📋 实施步骤总览

```
阶段1: 准备工作（4小时）
  → 创建Figma文件结构
  → 导入设计令牌
  → 设置变量系统

阶段2: 设计基础（8小时）
  → 颜色样式
  → 字体样式
  → 阴影/模糊效果
  → 组件基础

阶段3: 组件库（16小时）
  → 基础组件（按钮、输入框等）
  → 内容组件（卡片、列表等）
  → 反馈组件（Toast、Modal等）
  → 导航组件

阶段4: 页面设计（20小时）
  → 主界面
  → 文件列表视图
  → 解压进度界面
  → 设置页面
  → 空状态/错误状态

阶段5: 响应式适配（8小时）
  → 移动端适配
  → 平板适配
  → 多分辨率验证

阶段6: 动效标注（4小时）
  → 动画参数标注
  → 交互流程图
  → 开发交付
```

**总计：60小时 ≈ 1.5周（全职）**

---

## 一、准备工作（第1天，4小时）

### Step 1.1 创建Figma文件结构（30分钟）

```
Unzip App Design System/
│
├── 📄 Cover (封面)
│   └── 项目信息、设计理念说明
│
├── 🎨 Foundation (设计基础)
│   ├── Colors (色彩系统)
│   ├── Typography (字体系统)
│   ├── Spacing (间距系统)
│   ├── Shadows (阴影系统)
│   └── Effects (特效：光晕、模糊、渐变)
│
├── 🧩 Components (组件库)
│   ├── Primitives (基础组件)
│   ├── Content (内容组件)
│   ├── Feedback (反馈组件)
│   ├── Navigation (导航组件)
│   └── Forms (表单组件)
│
├── 📱 Screens (页面设计)
│   ├── Desktop (桌面端)
│   ├── Tablet (平板)
│   └── Mobile (移动端)
│
├── 🎬 Animations (动效标注)
│   └── 动画流程、参数标注
│
└── 📦 Exports (设计交付)
    ├── Developer Handoff
    └── Assets
```

**操作**：在Figma创建新文件，建立这些页面

---

### Step 1.2 安装必需插件（15分钟）

#### 必装插件

```
1. Tokens Studio (Design Tokens)
   → 导入设计令牌
   → 同步CSS变量
   
2. Autoflow
   → 绘制交互流程图
   
3. Similayer
   → 批量重命名图层
   
4. Color Contrast Checker
   → 验证WCAG对比度
   
5. A11y - Color Contrast Checker
   → 无障碍验证
```

**操作**：在Figma插件市场搜索安装

---

### Step 1.3 设置Figma变量系统（2小时）

#### 创建颜色变量

**参考文档**: `01-color-system.md`

```
Variables → Create collection: "Colors"

Mode 1: Light
Mode 2: Dark

变量组：
1. Primary (主色)
   - primary/50 → #ecfeff
   - primary/100 → #cffafe
   ...
   - primary/500 → #06b6d4 ⭐
   - primary/900 → #164e63

2. Neutral (中性色)
   - neutral/bg-primary → #ffffff (Light) / #0f172a (Dark)
   - neutral/text-primary → #0f172a (Light) / #f1f5f9 (Dark)

3. Semantic (语义色)
   - success → #10b981
   - error → #ef4444
   - warning → #f59e0b
   - info → #3b82f6

4. Aurora (极光辅助色)
   - aurora/purple → #a78bfa
   - aurora/pink → #f472b6
   - aurora/orange → #fb923c
   - aurora/green → #34d399
```

**操作**：
1. 打开 Variables 面板
2. 逐个创建颜色变量
3. 设置Light/Dark两个模式
4. 按文档中的色值填充

---

#### 创建数值变量

**参考文档**: `03-spacing-layout.md`, `09-design-tokens.md`

```
Variables → Create collection: "Spacing"

- spacing/1 → 4
- spacing/2 → 8
- spacing/3 → 12
- spacing/4 → 16
- spacing/6 → 24
- spacing/8 → 32
- spacing/12 → 48
- spacing/16 → 64

Variables → Create collection: "Radius"

- radius/lg → 8
- radius/xl → 12
- radius/2xl → 16
- radius/full → 9999
```

---

#### 创建效果样式（Effects）

**参考文档**: `09-design-tokens.md` 阴影令牌部分

```
在 Styles 面板创建：

1. 标准阴影
   - shadow/sm
   - shadow/md
   - shadow/lg

2. 无界光晕阴影 ⭐
   - shadow/glow-sm
     → Type: Drop shadow
     → X: 0, Y: 0, Blur: 10, Spread: 0
     → Color: #06b6d4, Opacity: 20%
   
   - shadow/glow-md
     → Blur: 20, Opacity: 30%
   
   - shadow/glow-lg
     → Blur: 30, Opacity: 40%
   
   - shadow/glow-layered
     → Layer 1: Blur: 20, Opacity: 30%
     → Layer 2: Blur: 40, Opacity: 15%

3. 无界容器阴影
   - shadow/borderless
     → Layer 1: X:0 Y:0 Blur:0 Spread:1, #000000 5%
     → Layer 2: X:0 Y:8 Blur:32 Spread:0, #000000 8%
   
   - shadow/borderless-hover
     → Layer 1: X:0 Y:0 Blur:0 Spread:1, #06b6d4 10%
     → Layer 2: X:0 Y:0 Blur:30, #06b6d4 15%
     → Layer 3: X:0 Y:16 Blur:48, #000000 12%
```

**操作**：
1. 选中任意形状
2. 右侧面板 → Effects → Drop shadow
3. 设置参数后，点击样式图标 → Create style
4. 命名为 `shadow/glow-md`

---

#### 创建背景模糊效果

```
在 Styles 面板创建：

- blur/glass (20)
  → Type: Background blur
  → Blur: 20
  
- blur/overlay (30)
  → Blur: 30
  
- blur/modal (24)
  → Blur: 24
```

---

### Step 1.4 准备设计素材（1小时）

#### 图标库选择

**推荐**：
1. **Lucide Icons** (免费，与文档中使用一致)
   - Figma插件：直接搜索插入
   - 网站：lucide.dev
   
2. **Iconoir** (免费，风格清新)
   
3. **或购买** Nucleo Icons (付费，3000+图标)

**操作**：安装图标插件，准备常用图标

#### 字体准备

**参考文档**: `02-typography.md`

```
安装字体：
1. Inter (主字体)
   → Google Fonts 下载
   → 字重：300, 400, 500, 600, 700
   
2. JetBrains Mono (等宽字体)
   → 用于文件名、大小、时间戳
```

**操作**：下载字体，安装到系统

---

## 二、设计基础页面（第1-2天，8小时）

### Step 2.1 色彩系统页面（2小时）

**参考文档**: `01-color-system.md`

**创建内容**：

```
页面：Foundation / Colors

布局：
┌─────────────────────────────────────┐
│ 🎨 色彩系统 - 水色光谱                │
├─────────────────────────────────────┤
│                                     │
│ 主色 - Cyan Gradient                │
│ ┌────┬────┬────┬────┬────┐          │
│ │ 50 │100 │200 │...│900 │          │
│ └────┴────┴────┴────┴────┘          │
│                                     │
│ 辅助色 - Aurora Palette             │
│ ┌────┬────┬────┬────┐               │
│ │紫色│粉色│橙色│绿色│               │
│ └────┴────┴────┴────┘               │
│                                     │
│ 中性色 - Deep Sea Gradient          │
│ ┌────────────────────┐              │
│ │ Slate 50 → 900     │              │
│ └────────────────────┘              │
│                                     │
│ 渐变展示                             │
│ ┌──────────────────────┐            │
│ │ gradient-primary     │            │
│ │ gradient-glow        │            │
│ │ gradient-flow        │            │
│ └──────────────────────┘            │
└─────────────────────────────────────┘
```

**操作步骤**：
1. 创建色块矩阵（每个颜色一个矩形）
2. 应用变量（使用刚才创建的颜色变量）
3. 添加色值标注（HEX、RGB、变量名）
4. 创建渐变示例（使用gradient fill）
5. 标注使用场景文字

---

### Step 2.2 字体系统页面（1.5小时）

**参考文档**: `02-typography.md`

```
页面：Foundation / Typography

内容：
1. 字体家族展示
   Inter Regular/Medium/Semibold/Bold
   JetBrains Mono

2. 字阶系统（10个）
   ┌──────────────────────┐
   │ Display (48px)       │
   │ Heading 1 (36px)     │
   │ Heading 2 (30px)     │
   │ ...                  │
   │ Caption (12px)       │
   └──────────────────────┘

3. 行高、字重、间距示例

4. 极简文字规范
   ✅ "上传" (2字)
   ✅ "开始解压" (4字)
   ❌ "上传文件到服务器" (9字)
```

**操作**：
1. 创建文本样式（Text Styles）
2. 命名：`text/5xl-bold`, `text/base-normal`
3. 添加使用示例
4. 标注字数限制规范

---

### Step 2.3 间距系统页面（1小时）

**参考文档**: `03-spacing-layout.md`

```
页面：Foundation / Spacing

创建8px网格可视化：
┌─────────────────────────────┐
│ 8px Grid System             │
│                             │
│ ▓ 4px (spacing-1)          │
│ ▓▓ 8px (spacing-2)         │
│ ▓▓▓ 12px (spacing-3)       │
│ ▓▓▓▓ 16px (spacing-4)      │
│ ...                         │
└─────────────────────────────┘

应用示例：
┌──────────────────┐
│  ← 24px →       │  按钮内边距
│  [  按钮  ]     │
│  ← 24px →       │
└──────────────────┘
```

**操作**：
1. 创建间距示意图
2. 用Auto Layout展示实际应用
3. 标注变量名

---

### Step 2.4 阴影与效果页面（3.5小时）⭐ 核心

**参考文档**: `09-design-tokens.md` 阴影令牌部分

```
页面：Foundation / Shadows & Effects

重点展示无界效果：

1. 标准阴影对比
   ┌────────┐  ┌────────┐
   │ 传统   │  │ 无界   │
   │ border │  │ glow   │
   └────────┘  └────────┘

2. 光晕阴影系列
   ┌──────┐ ┌──────┐ ┌──────┐
   │glow  │ │glow  │ │glow  │
   │ -sm  │ │ -md  │ │ -lg  │
   └──────┘ └──────┘ └──────┘
   
3. 无界容器效果
   ┌────────────────────┐
   │                    │
   │  Borderless Card   │
   │  (径向渐变晕染)    │
   │                    │
   └────────────────────┘
   
4. 水滴晕染效果演示
   (使用渐变 + 模糊 + 阴影组合)
```

**操作步骤**（关键）：

**创建无界卡片效果**：
1. 创建矩形，圆角16px
2. **填充**：
   - Type: Radial gradient
   - 中心点：Center
   - 色标：
     * 0%: #FFFFFF, 90%
     * 70%: #FFFFFF, 70%
     * 90%: #FFFFFF, 30%
     * 100%: #FFFFFF, 0%
3. **Effects**：
   - Background blur: 10px
   - Drop shadow 1: X:0 Y:0 Blur:0 Spread:1, #000000 5%
   - Drop shadow 2: X:0 Y:8 Blur:32, #000000 8%
4. **保存为组件**：命名 `card/borderless`

**创建光晕按钮**：
1. 创建矩形，圆角12px
2. **填充**：
   - Type: Linear gradient
   - 角度：135°
   - 色标：#06b6d4 → #0ea5e9
3. **Effects**：
   - Drop shadow 1: X:0 Y:0 Blur:20, #06b6d4 30%
   - Drop shadow 2: X:0 Y:0 Blur:40, #06b6d4 15%
4. **创建Variant**：
   - Default (默认)
   - Hover (光晕增强)
   - Active (光晕收缩)
5. **保存为组件**：命名 `button/primary`

---

### Step 1.5 使用Tokens Studio插件（2小时）⭐ 重要

#### 准备令牌JSON文件

创建文件：`figma-tokens.json`

**参考文档**: `09-design-tokens.md` 第10.4节

```json
{
  "global": {
    "colors": {
      "primary": {
        "50": { "value": "#ecfeff", "type": "color" },
        "100": { "value": "#cffafe", "type": "color" },
        "500": { "value": "#06b6d4", "type": "color" },
        "900": { "value": "#164e63", "type": "color" }
      },
      "gradient": {
        "primary": {
          "value": "linear-gradient(135deg, #06b6d4 0%, #0ea5e9 100%)",
          "type": "color"
        }
      }
    },
    "spacing": {
      "1": { "value": "4px", "type": "spacing" },
      "2": { "value": "8px", "type": "spacing" },
      "4": { "value": "16px", "type": "spacing" },
      "6": { "value": "24px", "type": "spacing" }
    },
    "borderRadius": {
      "xl": { "value": "12px", "type": "borderRadius" },
      "2xl": { "value": "16px", "type": "borderRadius" }
    },
    "boxShadow": {
      "glow-md": {
        "value": {
          "x": 0,
          "y": 0,
          "blur": 20,
          "spread": 0,
          "color": "rgba(6, 182, 212, 0.3)",
          "type": "dropShadow"
        },
        "type": "boxShadow"
      },
      "borderless": {
        "value": [
          {
            "x": 0,
            "y": 0,
            "blur": 0,
            "spread": 1,
            "color": "rgba(0, 0, 0, 0.05)",
            "type": "dropShadow"
          },
          {
            "x": 0,
            "y": 8,
            "blur": 32,
            "spread": 0,
            "color": "rgba(0, 0, 0, 0.08)",
            "type": "dropShadow"
          }
        ],
        "type": "boxShadow"
      }
    },
    "blur": {
      "glass": { "value": "20", "type": "blur" },
      "overlay": { "value": "30", "type": "blur" }
    }
  }
}
```

**操作**：
1. 创建 `figma-tokens.json` 文件
2. 在Figma中打开Tokens Studio插件
3. Import tokens → 选择文件
4. Apply to document
5. 所有变量自动创建！

---

## 三、组件库设计（第3-6天，16小时）

### Step 3.1 基础组件（6小时）

#### 按钮组件 ⭐ 关键

**参考文档**: `05-component-library.md` 第2.1节

**创建流程**：

1. **主按钮 (Primary Button)**

```
Frame: button/primary
Size: Auto width × 48px

结构：
┌──────────────────────────┐
│ ← 24px → [Icon] Text ← 24px → │
│          (18px) (14px)   │
└──────────────────────────┘

填充：
  - Gradient: 135°
  - #06b6d4 → #0ea5e9

效果：
  - Background blur: 不需要（实色渐变）
  - Drop shadow: 应用 shadow/glow-md

文字：
  - Style: text/sm-medium
  - Color: #ffffff
  - 内容："上传" (2字示例)

图标：
  - Lucide/upload
  - Size: 18×18
  - Color: #ffffff

创建Variants:
  Property: State
  Values: Default, Hover, Active, Disabled
  
  Hover variant:
    - Effects: 改为 shadow/glow-lg
    - Transform: translateY(-2px)
  
  Active variant:
    - Effects: 改为 shadow/glow-sm
    - Transform: scale(0.98)
  
  Disabled variant:
    - Opacity: 50%
    - Effects: none
```

**操作**：
1. 创建Frame，命名 `button/primary`
2. 添加Auto Layout（水平，居中对齐）
3. 间距：24px (左) - 8px (图标间距) - 24px (右)
4. 添加渐变背景
5. 应用阴影样式
6. 创建Component
7. 添加Variants（State: Default/Hover/Active/Disabled）
8. 每个Variant调整阴影和transform

---

2. **图标按钮 (Icon Button)**

```
Frame: button/icon
Size: 44×44px (触摸友好)

结构：
┌──────┐
│      │
│ Icon │ 居中
│      │
└──────┘

填充：
  - Radial gradient
  - Center: rgba(255,255,255,0.9)
  - Edge: transparent
  
效果：
  - Background blur: 8px
  - shadow/borderless

Variants:
  - Size: sm(32×32) / md(44×44) / lg(56×56)
  - State: Default / Hover / Active
  
Hover:
  - 背景改为 rgba(6,182,212,0.1)
  - shadow/glow-sm
```

---

3. **输入框 (Input)**

**参考文档**: `05-component-library.md` 第2.2节

```
Frame: input/default
Size: 320×48px

结构：
┌────────────────────────────┐
│ [Icon] Placeholder text    │
│ (18px)  (14px)             │
└────────────────────────────┘

填充：
  - Solid: rgba(255,255,255,0.7)
  
效果：
  - Background blur: 10px
  - shadow/borderless

Variants:
  - State: Default / Focus / Error / Disabled
  
Focus variant:
  - Effects: shadow/glow-md
  - Stroke: 2px, cyan-500/20%
```

---

#### Auto Layout 设置技巧

**所有组件都用Auto Layout**：

```
按钮设置：
  - Direction: Horizontal
  - Gap: 8px (图标与文字间距)
  - Padding: 12px vertical, 24px horizontal
  - Alignment: Center, Center
  - Hug contents: Horizontal
  - Fixed: Vertical (48px)

卡片设置：
  - Direction: Vertical
  - Gap: 16px (内部元素间距)
  - Padding: 24px (all sides)
  - Alignment: Top, Left
  - Hug contents: Both
```

---

### Step 3.2 内容组件（4小时）

#### 无界卡片组件 ⭐ 核心

**参考文档**: `05-component-library.md` 第3.1节

```
Component: card/borderless

尺寸：320×auto (Auto Layout)

结构：
┌───────────────────────────┐
│ ← 24px padding →          │
│                           │
│  [Icon]  Title            │
│  (48px)  (20px, semibold) │
│                           │
│  Description              │
│  (14px, secondary color)  │
│                           │
│  ────── (divider) ──────  │ ← 渐变分隔线
│                           │
│  [Icon] [Icon] [Icon]     │ ← 纯图标操作
│                           │
└───────────────────────────┘

关键设置：
  
填充（径向渐变）：
  - Type: Radial
  - Ratio: 1.5:1 (椭圆)
  - 色标：
    * 0%: #FFFFFF 90%
    * 70%: #FFFFFF 70%
    * 90%: #FFFFFF 30%
    * 100%: #FFFFFF 0%

效果：
  - Background blur: 10px
  - shadow/borderless

渐变分隔线：
  - 高度：1px
  - 填充：Linear gradient
  - 角度：0° (水平)
  - 色标：
    * 0%: transparent
    * 50%: #cbd5e1 50%
    * 100%: transparent

Variants:
  - State: Default / Hover
  
  Hover:
    - Effects: shadow/borderless-hover
    - Transform: translateY(-4px)
```

**操作重点**：
1. 径向渐变是关键！必须设置椭圆比例
2. 背景模糊要勾选 "Background blur"
3. 分隔线用Linear gradient，透明→颜色→透明
4. 图标操作用纯图标，不加文字

---

### Step 3.3 反馈组件（3小时）

**参考文档**: `05-component-library.md` 第4节

创建：
- Toast通知（边缘消融效果）
- 模态框（晕染边缘）
- 液滴加载器
- 进度条（液体流动）

**液滴加载器制作**：
```
1. 创建12×12px圆形
2. 调整一个角为尖角（水滴形状）
   - 选中左下角节点
   - 调整圆角为0
3. 旋转-45°
4. 填充：gradient-primary
5. Effects：shadow/glow-sm
6. 复制3个，创建Component
7. 用Prototype连接，设置循环动画
```

---

### Step 3.4 表单组件（3小时）

创建：
- 复选框（勾选时光晕）
- 开关（无边框轨道）
- 文件上传区（拖拽态晕染）

**上传区制作** ⭐：
```
Frame: dropzone/default
Size: 640×256px

填充（径向渐变）：
  - Radial gradient
  - 色标：
    * 0%: #FFFFFF 90%
    * 70%: #FFFFFF 60%
    * 100%: transparent

效果：
  - Background blur: 10px
  - shadow/borderless

中心内容：
  ┌─────────────┐
  │   Icon      │ 64×64px，青色
  │  (Upload)   │ 有光晕背景
  │             │
  │  "拖入文件"  │ 18px, semibold
  │  "或点击选择" │ 14px, secondary
  └─────────────┘

Variants:
  - State: Default / Dragging
  
  Dragging variant:
    - 渐变中心改为青色（rgba(6,182,212,0.2)）
    - 阴影改为 shadow/glow-xl
    - Transform: scale(1.02)
```

---

## 四、页面设计（第7-12天，20小时）

### Step 4.1 主界面设计（8小时）

**参考文档**: `!!requirement_v0.2.md` + 所有组件文档

**布局**：

```
┌─────────────────────────────────────────────┐
│ [Logo] Unzip        [搜索] [设置] [用户]    │ ← 导航栏（无界）
├─────────────────────────────────────────────┤
│                                             │
│              ┌────────────────┐            │
│              │                │            │
│              │   拖入文件     │            │ ← 上传区
│              │  (Upload Icon) │            │   (无界晕染)
│              │                │            │
│              └────────────────┘            │
│                                             │
│   最近解压 ────────────────────             │ ← 区块标题
│                                             │
│   ┌──────┐ ┌──────┐ ┌──────┐              │
│   │ File │ │ File │ │ File │              │ ← 文件卡片
│   │ Card │ │ Card │ │ Card │              │   (无界)
│   └──────┘ └──────┘ └──────┘              │
│                                             │
└─────────────────────────────────────────────┘
```

**设计要点**：

1. **背景**：
   - 渐变：from-slate-50 to-cyan-50/30
   - 或使用粒子背景（透明度30%）

2. **导航栏**：
   - 无边框！
   - backdrop-blur: 12px
   - 阴影：shadow/borderless
   - 底部光晕线（1px渐变）

3. **上传区**：
   - 使用刚才创建的 dropzone/default 组件
   - 放大到合适尺寸

4. **文件卡片**：
   - 使用 card/borderless 组件
   - 3列网格，gap:24px

5. **操作图标**：
   - 全部用纯图标
   - 无文字标签

---

### Step 4.2 文件列表视图（4小时）

**参考文档**: `05-component-library.md` 第3.2节

```
Layout:
┌─────────────────────────────────────┐
│ [搜索框]  [过滤] [排序] [视图]      │ ← 工具栏（纯图标）
├─────────────────────────────────────┤
│                                     │
│ ┌─┬─────────────────┬──────┬─────┐ │
│ │✓│ project.zip     │24 MB │操作 │ │ ← 文件项
│ └─┴─────────────────┴──────┴─────┘ │   (无界)
│                                     │
│ ┌─┬─────────────────┬──────┬─────┐ │
│ │ │ backup.rar      │156MB │操作 │ │
│ └─┴─────────────────┴──────┴─────┘ │
│                                     │
│ ... 更多文件 ...                    │
│                                     │
└─────────────────────────────────────┘

每个文件项：
  - 背景：rgba(255,255,255,0.8)
  - 模糊：blur(8px)
  - 阴影：borderless
  - 悬停：光晕增强
  - 操作按钮：纯图标（下载、更多）
```

---

### Step 4.3 解压进度界面（4小时）

**参考文档**: `04-animation-specs.md` 液体进度条

```
Modal layout:
┌───────────────────────────┐
│                           │
│    [Liquid Progress]      │ ← 液体进度环
│         65%               │   (需手绘)
│                           │
│      解压中               │ ← 3字
│   245/512 文件            │
│                           │
│  ▓▓▓▓▓▓▓▓▓░░░░░░          │ ← 液体进度条
│                           │
│         [取消]            │ ← 2字按钮
│                           │
└───────────────────────────┘

背景：
  - 径向渐变晕染
  - 模糊：blur(24px)
  - 阴影：shadow/borderless-modal
```

**液体进度条制作**：
1. 容器：圆角100px，高8px，背景 cyan-500/10%
2. 填充：
   - 渐变：90° linear
   - 色标：#06b6d4 → #22d3ee → #06b6d4
   - 设置为200%宽度（overflow hidden）
3. 效果：
   - 内阴影模拟高光
   - 外光晕 shadow/glow-md

---

### Step 4.4 空状态与错误（2小时）

**参考文档**: `05-component-library.md` 第4.4节

```
空状态：
  - 图标带光晕背景（径向渐变圆形）
  - 标题："暂无文件" (4字)
  - 描述："拖入压缩包开始" (7字)
  - 按钮："选择文件" (4字)

错误状态：
  - 红色图标光晕
  - 标题："文件损坏" (4字)
  - 方案："无法读取" (4字)
  - 按钮："重新下载" (4字)
```

---

### Step 4.5 设置页面（2小时）

```
侧边栏导航（纯图标或图标+2字）：
  - [Icon] 通用
  - [Icon] 外观
  - [Icon] 快捷键
  - [Icon] 关于

内容区：
  - 开关组件
  - 选择框
  - 滑块
  - 全部无界设计
```

---

## 五、响应式适配（第13-14天，8小时）

### Step 5.1 创建响应式Frame

**参考文档**: `07-responsive-design.md`

```
在 Screens 页面创建：

Desktop (1440×900)
  → 完整布局
  → 所有光晕效果
  → 侧边栏展开

Tablet (768×1024)
  → 2列网格
  → 简化光晕
  → 侧边栏收起

Mobile (375×812)
  → 单列布局
  → 最简光晕
  → 底部操作栏
```

**操作**：
1. 创建3个Frame（不同尺寸）
2. 复制主界面内容
3. 调整布局（网格列数、间距）
4. 简化效果（移动端减少阴影层数）

---

### Step 5.2 使用Auto Layout响应式

```
技巧：用Min/Max width实现响应

例如：文件网格
  - Desktop: 4列
    → Container: Min width 1024px
    → Grid: 4 columns, gap 32px
  
  - Tablet: 2列
    → Container: Min 768px, Max 1023px
    → Grid: 2 columns, gap 24px
  
  - Mobile: 1列
    → Container: Max 767px
    → Grid: 1 column, gap 16px
```

---

## 六、动效标注（第15天，4小时）

### Step 6.1 使用Prototype创建交互流

**参考文档**: `06-interaction-patterns.md`

```
关键交互流：

1. 上传流程
   [空状态] 
     → On click → [文件选择器]
     → On drop → [上传进度]
     → After delay → [文件列表]

2. 解压流程
   [文件卡片]
     → On click → [预览]
     → On button click → [解压进度]
     → After delay → [成功Toast]

3. 删除流程
   [文件项]
     → On long press → [选中态]
     → On delete → [确认对话框]
     → On confirm → [删除动画]
```

**操作**：
1. 切换到Prototype模式
2. 连接交互（drag蓝色箭头）
3. 设置动画：
   - Animation: Smart animate
   - Easing: Ease out
   - Duration: 300ms

---

### Step 6.2 动画参数标注

**在每个有动画的组件旁边添加注释**：

```
Button Hover:
  ┌──────────────┐
  │   [Button]   │
  └──────────────┘
  
  动画参数 →
    Duration: 150ms
    Easing: ease-out
    Transform: translateY(-2px)
    Shadow: glow-md → glow-lg
    
涟漪效果 →
    Duration: 800ms
    Easing: ease-out
    Scale: 0 → 400px
    Opacity: 0.6 → 0
```

**使用文本框或Sticky notes标注**

---

## 七、开发交付（第16天，4小时）

### Step 7.1 整理Dev Mode

**在Figma中设置Dev Mode信息**：

1. **组件说明**：
```
在组件描述中写：
  Component: button/primary
  File: 05-component-library.md 第2.1节
  
  CSS实现：
  background: linear-gradient(135deg, #06b6d4, #0ea5e9);
  box-shadow: 0 0 20px rgba(6, 182, 212, 0.3);
  border: none; /* 无界设计 */
  
  交互：
  - Hover: 光晕增强，上浮2px
  - Active: 光晕收缩，缩小至0.98
  - Duration: 150ms
  - Easing: cubic-bezier(0, 0, 0.2, 1)
```

2. **导出设置**：
```
图标：
  - Format: SVG
  - Naming: icon-{name}
  
图片：
  - Format: WebP
  - Scale: @1x, @2x, @3x
  
组件：
  - Copy as CSS/React code
```

---

### Step 7.2 创建开发交付文档

**在Figma中创建 "Developer Handoff" 页面**：

```
内容：

1. 文档引用
   - docs/ux/00-design-concept.md ⭐ 设计总纲
   - docs/ux/09-design-tokens.md → CSS变量
   - docs/ux/05-component-library.md → 组件代码

2. 关键设计原则
   ✅ 无边框（用光晕替代）
   ✅ 极简文字（按钮≤4字）
   ✅ 流体动效（fluid缓动）

3. Tailwind配置
   → 复制 09-design-tokens.md 的配置
   
4. 颜色变量表
   → 所有颜色的HEX、RGB、CSS变量名

5. 间距系统
   → 8px网格图示

6. 动画参数速查表
   → 时长、缓动、延迟
```

---

### Step 7.3 导出资源

**图标导出**：
```
1. 选中所有图标
2. Export settings
   - Format: SVG
   - Outline stroke: Yes
   - Naming: icon/{name}
3. Export all

结果：icon/upload.svg, icon/download.svg...
```

**设计令牌导出**：
```
使用 Tokens Studio 插件：

1. Export tokens
2. Format: JSON
3. 保存为 design-tokens.json
4. 给开发团队，可直接导入Tailwind
```

---

## 八、高效技巧与工具

### 🚀 加速技巧

#### 1. 使用Figma插件加速

```
1. Iconify (图标插件)
   → 搜索 "upload" 直接插入
   → 统一调整尺寸和颜色

2. Content Reel (假数据生成)
   → 快速填充文件名、日期
   → 但要确保极简文字规范

3. Stark (无障碍检查)
   → 自动检查对比度
   → 验证WCAG标准

4. Remove BG (去背景)
   → 处理图片资源

5. Unsplash (免费图片)
   → 快速获取占位图
```

---

#### 2. 创建Component Library模板

**先创建这些基础组件**：
```
优先级：
1. ⭐⭐⭐ 必须：
   - button/primary
   - button/secondary
   - button/icon
   - input/default
   - card/borderless
   - modal/default
   - toast/success
   - toast/error

2. ⭐⭐ 重要：
   - checkbox
   - switch
   - dropdown
   - tooltip
   - divider

3. ⭐ 可选：
   - timeline
   - rating
   - slider
```

**然后组合成页面**，而非从头画

---

#### 3. 使用Variants管理状态

**所有交互组件都用Variants**：

```
button/primary
  ├─ State
  │  ├─ Default
  │  ├─ Hover (光晕增强)
  │  ├─ Active (光晕收缩)
  │  └─ Disabled (半透明)
  │
  └─ Size
     ├─ sm
     ├─ md
     └─ lg

自动生成 4×3 = 12个变体！
```

**好处**：
- 开发者看到所有状态
- 自动生成代码
- 易于维护

---

### 🎯 关键参数速查

#### 无界卡片径向渐变

```
在Figma中设置：
1. Fill → Radial gradient
2. 点击渐变条
3. 添加4个色标：
   
   位置   颜色              透明度
   0%    #FFFFFF          90%
   70%   #FFFFFF          70%
   90%   #FFFFFF          30%
   100%  #FFFFFF          0%

4. 调整椭圆比例：宽1.5 : 高1
```

#### 光晕阴影

```
在Figma中设置：
1. Effects → Drop shadow
2. 参数：
   
   层1（内层光晕）:
   X: 0
   Y: 0
   Blur: 20
   Spread: 0
   Color: #06b6d4
   Opacity: 30%
   
   层2（外层扩散）:
   X: 0
   Y: 0
   Blur: 40
   Spread: 0
   Color: #06b6d4
   Opacity: 15%
```

#### 背景模糊

```
在Figma中设置：
1. Effects → Background blur
2. Blur amount: 10 (卡片) / 20 (玻璃) / 30 (遮罩)
```

---

## 九、完整工作流示例

### 创建一个完整的"无界按钮"

**从文档到Figma的完整流程**：

#### 📖 Step 1: 查阅文档

打开 `05-component-library.md` 第2.1节：
```javascript
<ButtonPrimary icon={UploadIcon}>上传</ButtonPrimary>

style={{
  background: 'linear-gradient(135deg, #06b6d4, #0ea5e9)',
  boxShadow: '0 0 20px rgba(6, 182, 212, 0.3)',
}}

hover: {
  boxShadow: '0 0 30px rgba(6, 182, 212, 0.4)',
  transform: 'translateY(-2px)',
}
```

---

#### 🎨 Step 2: 在Figma实现

1. **创建Frame**
   - 名称：`button/primary`
   - Size：Auto × 48px
   - Auto Layout：Horizontal, gap 8px, padding 12px 24px

2. **添加图标**
   - 插件：Iconify → 搜索 "upload"
   - 调整尺寸：18×18px
   - 颜色：#FFFFFF

3. **添加文字**
   - 内容："上传" (2字) ✅
   - Style：text/sm-medium
   - Color：#FFFFFF

4. **设置填充**
   - Type：Linear gradient
   - Angle：135°
   - 色标：#06b6d4 (0%) → #0ea5e9 (100%)

5. **设置阴影**
   - Effects → Drop shadow
   - X:0 Y:0 Blur:20 Spread:0
   - Color：#06b6d4, Opacity:30%

6. **创建Component** (Cmd+Option+K)

7. **添加Variants**
   - 右侧面板 → Add variant property → "State"
   - 值：Default, Hover, Active, Disabled

8. **设置Hover变体**
   - 选中Hover variant
   - 修改阴影 Blur: 30, Opacity: 40%
   - 添加注释："translateY(-2px) 需开发实现"

9. **设置Active变体**
   - 选中Active variant
   - 修改阴影 Blur: 10, Opacity: 30%
   - 添加注释："scale(0.98)"

10. **设置Disabled变体**
    - Opacity: 50%
    - 移除所有阴影

---

#### 🔗 Step 3: 关联文档

在组件描述中写：
```
Primary Button - 主按钮（无界设计）

设计文档：
- 理念：docs/ux/00-design-concept.md (无界设计语言)
- 规范：docs/ux/05-component-library.md 第2.1节
- 令牌：docs/ux/09-design-tokens.md

设计特点：
- ❌ 无边框
- ✅ 青色渐变背景
- ✅ 光晕阴影
- ✅ 悬停时光晕增强

文字规范：
- 最多4个字
- 示例："上传"、"删除"、"开始解压"

动画：
- Duration: 150ms
- Easing: ease-out (cubic-bezier(0, 0, 0.2, 1))
```

---

## 十、质量检查清单

### 设计稿完成前验证

#### 无界设计验证
- [ ] 所有组件无硬性边框（border: none）
- [ ] 使用径向渐变或光晕阴影
- [ ] 背景都有模糊效果（blur）
- [ ] 悬停态有光晕变化
- [ ] 分隔线都是渐变（透明→颜色→透明）

#### 极简文字验证
- [ ] 所有按钮文字≤4个字
- [ ] 工具栏使用纯图标
- [ ] 标签/提示≤6个字
- [ ] 空状态文案≤12个字
- [ ] 无"请"、"您"等虚词

#### 色彩验证
- [ ] 主色统一使用Cyan (#06b6d4)
- [ ] 辅助色使用极光色盘
- [ ] 中性色使用Slate系列
- [ ] 对比度≥4.5:1（用插件验证）

#### 间距验证
- [ ] 所有间距都是8的倍数
- [ ] 使用spacing变量
- [ ] 按钮内边距：12px × 24px
- [ ] 卡片内边距：24px

#### 组件完整性
- [ ] 所有组件都有Variants（至少Default/Hover）
- [ ] 图标按钮有aria-label标注
- [ ] 复杂组件有使用说明
- [ ] 响应式有3个尺寸

#### 开发交付
- [ ] 所有组件可在Dev Mode查看代码
- [ ] 关键参数有标注
- [ ] 图标可导出SVG
- [ ] 有完整的组件库页面

---

## 十一、推荐的实施顺序

### 🎯 优先级排序

#### Week 1: 基础与核心组件
```
Day 1-2: 设计基础
  ✅ 颜色系统页面
  ✅ 字体系统页面
  ✅ 阴影效果页面
  ✅ 导入设计令牌

Day 3-4: 核心组件
  ✅ 按钮（4种）
  ✅ 输入框
  ✅ 无界卡片 ⭐
  ✅ 文件项组件

Day 5: 反馈组件
  ✅ Toast
  ✅ Modal
  ✅ 加载器
```

#### Week 2: 页面与交付
```
Day 6-7: 主要页面
  ✅ 主界面（上传区+文件列表）
  ✅ 解压进度界面

Day 8: 响应式
  ✅ 移动端适配
  ✅ 平板适配

Day 9: 动效标注
  ✅ Prototype交互
  ✅ 参数标注

Day 10: 交付准备
  ✅ Dev Mode整理
  ✅ 导出资源
  ✅ 文档关联
```

---

## 十二、与AI协作技巧

### 使用ChatGPT/Claude辅助

#### 生成Figma Plugin代码

```
Prompt示例：

"帮我写一个Figma插件代码，批量创建无界卡片组件：
- 径向渐变背景（中心90%白色，边缘透明）
- 背景模糊10px
- 阴影：0 0 0 1px rgba(0,0,0,0.05), 0 8px 32px rgba(0,0,0,0.08)
- 圆角16px
- 内边距24px"
```

#### 生成令牌JSON

```
Prompt：

"根据这个CSS变量列表，生成Figma Tokens Studio的JSON格式：
[粘贴 09-design-tokens.md 中的CSS变量]"
```

#### 生成组件描述

```
Prompt：

"把这段组件代码转成Figma组件描述：
[粘贴 05-component-library.md 中的代码]"
```

---

## 十三、常见问题与解决

### Q1: Figma能实现径向渐变吗？
**A**: ✅ 可以！
- Fill → Radial gradient
- 调整椭圆比例和色标
- 可以实现水滴晕染效果

### Q2: Figma能做背景模糊吗？
**A**: ✅ 可以！
- Effects → Background blur
- 设置模糊度（10-30px）
- 需要半透明背景才有效果

### Q3: Figma能做多层阴影吗？
**A**: ✅ 可以！
- Effects → Add effect → Drop shadow
- 可以叠加4层
- 实现光晕扩散效果

### Q4: 如何做渐变分隔线？
**A**: 
- 创建1px高的矩形
- Fill → Linear gradient 0°
- 色标：transparent → color → transparent

### Q5: 如何标注动画参数？
**A**: 
- 用Sticky notes标注
- 或在组件描述中写明
- 推荐用Prototype + Comment

### Q6: 如何与开发协作？
**A**: 
- 启用Dev Mode
- Share link时勾选 "Can inspect"
- 开发者可直接看到CSS代码

---

## 十四、时间节省方案

### 🚀 如果时间紧张（30小时版本）

**删减内容**：
- ❌ 不做完整的动效标注（用文档替代）
- ❌ 不做所有页面（只做主界面+1个关键流程）
- ❌ 不做3个响应式尺寸（只做Desktop + Mobile）

**保留核心**：
- ✅ 设计基础页面（颜色、字体、阴影）
- ✅ 核心组件库（10-15个组件）
- ✅ 主界面设计
- ✅ 开发交付文档

**工作量**：2天密集工作可完成MVP

---

### ⚡ 如果非常紧急（8小时版本）

**仅做关键交付物**：
1. 主界面设计稿（1个）
2. 核心组件库（5个：按钮、卡片、输入框、Toast、Modal）
3. 颜色+阴影样式库
4. 开发交付说明

**方法**：
- 直接在Screens页面画主界面
- 边画边创建组件
- 不做完整的Foundation页面
- 不做响应式

---

## 十五、最终交付清单

### ✅ 必须交付

```
Figma文件包含：

1. 🎨 Foundation页面
   - 颜色系统 ✓
   - 字体系统 ✓
   - 阴影效果（含光晕）✓

2. 🧩 Component Library
   - 10-15个核心组件 ✓
   - 每个组件有Variants ✓
   - 无界设计特征 ✓

3. 📱 主界面设计稿
   - Desktop (1440×900) ✓
   - Mobile (375×812) ✓

4. 📦 Developer Handoff
   - 文档引用 ✓
   - 设计令牌导出 ✓
   - 关键参数标注 ✓

5. 🎬 关键交互流程
   - 上传流程 Prototype ✓
   - 解压流程 Prototype ✓
```

### 📤 导出文件

```
交付给开发团队：

1. Figma链接（Can inspect权限）
2. design-tokens.json
3. icons/ (SVG图标文件夹)
4. README.md (说明文档引用关系)
```

---

## 十六、实战：第一个组件完整演示

### 🎬 录制过程：创建无界主按钮

**时间：15分钟**

```
步骤：

00:00 - 创建Frame
  → 快捷键：F
  → 命名：button/primary
  → 设置Auto Layout (Shift+A)

01:00 - 设置尺寸
  → Padding: 12px (上下) 24px (左右)
  → Gap: 8px
  → Hug contents: Horizontal
  → Fixed height: 48px

02:00 - 添加图标
  → 插件：Iconify
  → 搜索：upload
  → 插入，调整为18×18px
  → 颜色：#FFFFFF

03:00 - 添加文字
  → 快捷键：T
  → 内容："上传" (2字) ✅
  → Font: Inter Medium
  → Size: 14px
  → Color: #FFFFFF

04:00 - 设置渐变背景
  → Fill → Linear gradient
  → Angle: 135°
  → Color 1: #06b6d4 (0%)
  → Color 2: #0ea5e9 (100%)

06:00 - 设置圆角
  → Corner radius: 12px

07:00 - 添加光晕阴影
  → Effects → Drop shadow
  → X:0 Y:0 Blur:20 Spread:0
  → Color: #06b6d4, Opacity: 30%

09:00 - 创建Component
  → 快捷键：Cmd+Option+K
  → 检查名称：button/primary

10:00 - 添加Variants
  → 右侧 → Add variant property
  → Name: "State"
  → Values: Default, Hover, Active, Disabled

11:00 - 设置Hover变体
  → 选中Hover variant
  → 修改阴影：Blur改为30, Opacity改为40%
  → 添加注释：hover时 translateY(-2px)

13:00 - 设置Active变体
  → 修改阴影：Blur:10, Opacity:30%
  → 添加注释：active时 scale(0.98)

14:00 - 设置Disabled变体
  → Opacity: 50%
  → 移除所有Effects

15:00 - 完成！
```

**成果**：一个完整的无界按钮组件，有4个状态

---

## 十七、推荐的学习路径

### 如果你不熟悉Figma

**建议学习顺序（10小时）**：

```
Hour 1-2: Figma基础
  - 界面布局
  - 基本工具（Frame、Shape、Text）
  - Auto Layout
  
Hour 3-4: 组件系统
  - Component创建
  - Variants使用
  - Instance管理
  
Hour 5-6: 样式系统
  - Color styles
  - Text styles
  - Effects styles
  - Variables（新功能）
  
Hour 7-8: 高级技巧
  - 渐变填充
  - 背景模糊
  - 多层阴影
  - Prototype
  
Hour 9-10: 插件使用
  - Tokens Studio
  - Iconify
  - Autoflow
```

**推荐教程**：
- Figma官方教程（YouTube）
- "Figma in 40 minutes" 快速入门

---

## 总结：你的行动计划

### 🎯 立即开始（今天）

```
1. 创建Figma账号/文件 (15分钟)
2. 安装必需插件 (15分钟)
3. 创建文件结构 (30分钟)
4. 导入第一批颜色变量 (1小时)

→ 今天完成：基础框架搭建
```

### 📅 本周完成（5天）

```
Day 1: 设计基础 + 变量系统
Day 2-3: 核心组件库（10个）
Day 4: 主界面设计
Day 5: 整理与交付准备

→ 本周完成：MVP设计稿
```

### 🚀 完整版（2周）

```
Week 1: 基础+组件+主要页面
Week 2: 响应式+动效+完善+交付

→ 两周完成：完整设计交付
```

---

**你现在有世界级的设计文档，只差最后一步将其可视化！**  
**Figma只是工具，你的文档才是灵魂。**  
**按这个指南，60小时内可完成专业级设计稿！** 🎨✨

**建议：先用2小时完成一个无界按钮，熟悉流程后再批量制作。** 💪

